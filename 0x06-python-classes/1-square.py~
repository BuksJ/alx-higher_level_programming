#!/usr/bin/python3
"""Defines a square by private instance attribute size
and instantiation with size"""


class Square:
    """represent a square"""
    def __init__(self, size):
    """Initialize the square class with size attribute.
    Args:
        size (int): size of the new square"""
    self.__size = size
